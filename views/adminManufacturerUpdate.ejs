<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Cyber Cars</title>
</head>
<body>
    <%- include('partials/navbar', {
        nav: {
            addCar: '/admin/car',
            addMnfctr: '/admin/manufacturer',
            cars: '/admin/car/update',
            manufacturers: '/admin/manufacturer/update',
            search: '/admin/search',
            users: '/admin/customerDetails',
            messages: '/admin/message',
            status: '/admin/carstatus',
            logout: '/admin/logout'
        },
        active: 'manufacturers'
    }) %>
    <div class="container">
        <form action="/admin/manufacturer/update/<%= data[0].manufacturerID %>" method="POST">
            <div>
                <div class="label">
                    <label for="name">Manufacturer Name</label>
                </div>
                <input type="text" id="name" name="name" value="<%= data[0].name %>">
            </div>
            <div>
                <div class="label">
                    <label for="country">Manufacturer Country</label>
                </div>
                <input type="text" id="country" name="country" value="<%= data[0].country %>">
            </div>

            <div>
                <button type="submit">Update</button>
            </div>
        </form>
    </div>
</body>
</html>